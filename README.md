الدليل الكامل لـ LuaU في روبلوكس من الألف للياء 🚀

خلينا نفهم كل حاجة عن لوا يو (Luau) في روبلوكس من الأول، من طباعة أول سطر كود لحد ما تعمل سيستم متكامل. دي اللغة الوحيدة اللي هتشتغل بيها في روبلوكس ستوديو.

---

١. الأساسيات: إزاي تبدأ تشتغل؟

الطباعة والخرج (Printing & Output)

```lua
-- أبسط شكل
print("أهلاً يا عالم!")

-- طبع متغيرات مع نص
local اسم_اللاعب = "أحمد"
local مستوي_اللعبة = 10
print("اللاعب " .. اسم_اللاعب .. " وصل للمستوى " .. مستوي_اللعبة)

-- طبع جدول (Table)
local جدول_اللاعب = {اسم = "محمد", نقاط = 100}
print(جدول_اللاعب.اسم) -- محمد
```

المتغيرات والأنواع (Variables & Data Types)

```lua
-- المتغيرات المحلية (Local Variables) - الأفضل والأسرع
local رقم = 42
local كلمة = "روبلكس"
local صح = true
local لا_شيء = nil

-- الثوابت (لو عايز حاجة متتغيرش)
local PI = 3.14159
local MAX_PLAYERS = 20

-- أنواع Roblox الخاصة
local موقع = Vector3.new(10, 5, 0) -- X, Y, Z
local لون = Color3.new(1, 0, 0) -- أحمر (R, G, B)
local إطار = CFrame.new(موقع) * CFrame.Angles(0, math.rad(45), 0)
```

---

٢. التحكم في تدفق البرنامج

الشروط (Conditionals)

```lua
local النقود = 750
local العمر = 16

-- if بس
if النقود > 1000 then
    print("غني")
end

-- if مع else
if العمر >= 18 then
    print("بالغ")
else
    print("قاصر")
end

-- شروط متعددة
if النقود > 500 and العمر > 12 then
    print("تقدر تشتري الحاجة دي")
elseif النقود < 100 or العمر < 10 then
    print("مش تقدر")
else
    print("حالة وسط")
end

-- الشرط القصير (Ternary-like)
local الحالة = (النقود > 500) and "غني" or "فقير"
```

الحلقات (Loops)

```lua
-- for loop مع الأرقام
for i = 1, 10 do
    print("دورة رقم: " .. i)
end

-- for loop تنازلي
for i = 10, 1, -1 do
    print(i)
end

-- loop في الجداول
local الفواكه = {"تفاح", "موز", "برتقال"}
for _, فاكهة in ipairs(الفواكه) do
    print("فاكهة: " .. فاكهة)
end

-- loop في الجداول بالمفاتيح
local معلومات = {الاسم = "خالد", العمر = 20, المدينة = "القاهرة"}
for مفتاح, قيمة in pairs(معلومات) do
    print(مفتاح .. ": " .. قيمة)
end

-- while loop
local عداد = 0
while عداد < 5 do
    print("العداد: " .. عداد)
    عداد = عداد + 1
    task.wait(1) -- استنى ثانية
end

-- repeat until
local الرقم = 0
repeat
    الرقم = الرقم + 1
    print(الرقم)
until الرقم >= 3
```

---

٣. الجداول: أقوى حاجة في لوا (Tables)

```lua
-- جدول عادي
local لاعب = {
    الاسم = "سارة",
    المستوى = 5,
    النقاط = 1000,
    المهارات = {"جري", "قفز", "هجوم"}
}

-- الوصول للقيم
print(لاعب.الاسم) -- سارة
print(لاعب["المستوى"]) -- 5
print(لاعب.المهارات[1]) -- جري

-- تعديل الجدول
لاعب.النقاط = 1500
لاعب.الموقع = Vector3.new(0, 10, 0)

-- حذف من الجدول
لاعب.المستوى = nil

-- دوال مفيدة مع الجداول
local الأرقام = {5, 3, 8, 1}
table.insert(الأرقام, 10) -- أضف في الآخر
table.remove(الأرقام, 2) -- احذف العنصر الثاني
table.sort(الأرقام) -- رتب الجدول

-- جيب حجم الجدول
print(#الأرقام) -- عدد العناصر
```

---

٤. الدوال: إعادة استخدام الكود (Functions)

```lua
-- دالة بسيطة
function اهلا()
    print("أهلاً!")
end
اهلا()

-- دالة مع باراميترات
function جمع(رقم1, رقم2)
    return رقم1 + رقم2
end
local الناتج = جمع(5, 3) -- 8

-- دالة مع باراميترات افتراضية
function ضرب(رقم1, رقم2)
    رقم2 = رقم2 or 1 -- لو رقم2 مش موجود، خليه 1
    return رقم1 * رقم2
end

-- دالة مع كذا ريتيرن
function احسب(رقم)
    return رقم * 2, رقم * 3, رقم * 4
end
local ضعف, ثلاثي, رباعي = احسب(5)

-- الدوال المجهولة (Anonymous Functions)
local ضعف = function(س)
    return س * 2
end
print(ضعف(4)) -- 8

-- الدوال كم باراميتر
function نفذ_الدالة(دالة, قيمة)
    return دالة(قيمة)
end
local النتيجة = نفذ_الدالة(ضعف, 10) -- 20
```

---

٥. أنواع البيانات المتقدمة في Roblox

Vector3 vs Vector2

```lua
-- Vector3: للمواضع في الفراغ ثلاثي الأبعاد
local موقع = Vector3.new(10, 5, -3)
print(موقع.X, موقع.Y, موقع.Z) -- 10 5 -3

-- Vector2: للشاشات ثنائية الأبعاد (UI)
local موقع_شاشة = Vector2.new(100, 200)
```

CFrame - أقوى من Vector3

```lua
-- CFrame للموضع فقط
local إطار1 = CFrame.new(10, 5, 0)

-- CFrame مع دوران
local إطار2 = CFrame.new(0, 0, 0) * CFrame.Angles(
    math.rad(45), -- X axis
    0,            -- Y axis
    math.rad(90)  -- Z axis
)

-- CFrame من محور واتجاه
local إطار3 = CFrame.fromAxisAngle(Vector3.new(0, 1, 0), math.rad(90))

-- تحويل CFrame لـ Vector3
local الموضع = إطار1.Position -- Vector3

-- تدوير CFrame حول نقطة
local إطار_مدور = إطار1:ToObjectSpace(إطار2)
```

Color3 و BrickColor

```lua
-- Color3 بـ RGB (0 إلى 1)
local أحمر = Color3.new(1, 0, 0)
local أخضر = Color3.new(0, 1, 0)
local أزرق = Color3.new(0, 0, 1)
local أبيض = Color3.new(1, 1, 1)

-- Color3 من Hex
local لون_من_هكس = Color3.fromHex("#FF5733")

-- BrickColor (الألوان الجاهزة في روبلوكس)
local لون_طوبي = BrickColor.new("Bright blue")
local لون_طوبي2 = BrickColor.Blue()

-- تحويل بينهم
local لون1 = لون_طوبي.Color -- BrickColor لـ Color3
local لون2 = BrickColor.new(أحمر) -- Color3 لـ BrickColor
```

---

٦. أحداث Roblox (Events)

```lua
-- حدث Touched (لمس)
local جزء = workspace.Part

local function لما_يلمس(الشيء_الملموس)
    local موديل_اللاعب = الشيء_الملموس:FindFirstAncestorOfClass("Model")
    if موديل_اللاعب then
        local لاعب = game.Players:GetPlayerFromCharacter(موديل_اللاعب)
        if لاعب then
            print(لاعب.Name .. " لمس الجزء!")
        end
    end
end

-- ربط الدالة بالحدث
الجزء.Touched:Connect(لما_يلمس)

-- حدث ClickDetector
local كاشف_الضغط = Instance.new("ClickDetector")
كاشف_الضغط.Parent = جزء

كاشف_الضغط.MouseClick:Connect(function(اللاعب)
    print(اللاعب.Name .. " ضغط على الجزء")
end)

-- حدث تغيير الخاصية
جزء:GetPropertyChangedSignal("Position"):Connect(function()
    print("الموضع اتغير!")
end)

-- حدث PlayerAdded
game.Players.PlayerAdded:Connect(function(لاعب)
    print("لاعب جديد انضم: " .. لاعب.Name)
end)
```

---

٧. الـ Roblox API الأساسية

```lua
-- جلب الخدمات
local Workspace = game:GetService("Workspace")
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local ServerScriptService = game:GetService("ServerScriptService")

-- إنشاء Instances جديدة
local جزء_جديد = Instance.new("Part")
جزء_جديد.Name = "جزءي"
جزء_جديد.Parent = Workspace
جزء_جديد.Position = Vector3.new(0, 10, 0)
جزء_جديد.BrickColor = BrickColor.new("Bright red")
جزء_جديد.Material = Enum.Material.Neon

-- البحث عن Instances
local الجزء = Workspace:FindFirstChild("جزءي")
local كل_الأجزاء = Workspace:GetChildren()
local جزء_باسم = Workspace:FindFirstChildWhichIsA("Part")

-- التعامل مع اللاعبين
local function لما_ينضم_لاعب(لاعب)
    -- عمل فار للاعب
    local فار_اللاعب = Instance.new("Folder")
    فار_اللاعب.Name = "معلومات_" .. لاعب.Name
    فار_اللاعب.Parent = ReplicatedStorage
    
    -- عمل القيم
    local النقاط = Instance.new("NumberValue")
    النقاط.Name = "النقاط"
    النقاط.Value = 0
    النقاط.Parent = فار_اللاعب
    
    local المستوى = Instance.new("IntValue")
    المستوى.Name = "المستوى"
    المستوى.Value = 1
    المستوى.Parent = فار_اللاعب
end

Players.PlayerAdded:Connect(لما_ينضم_لاعب)
```

---

٨. 30 نصيحة وخدعة احترافية

الأداء والسرعة (Performance)

1. خلي كل المتغيرات local - أسرع بكتير من المتغيرات العامة
2. Cache الخدمات والأشياء اللي بتستخدمها كتير
   ```lua
   local Workspace = game:GetService("Workspace")
   local Players = game:GetService("Players")
   ```
3. استخدم task بدل wait - أدق وأفضل في LuaU
4. استخدم table.create() لما تعرف حجم الجدول مسبقاً
5. قلل استخدام # في الجداول الكبيرة، خاصتاً في الحلقات
6. استخدم ipairs() للجداول العددية، pairs() للجداول النصية
7. خلي الدوال اللي في الحلقات برة الحلقة
   ```lua
   -- وحش
   for i = 1, 100 do
       local function دالة() end
       دالة()
   end
   
   -- كويس
   local function دالة() end
   for i = 1, 100 do
       دالة()
   end
   ```
8. استخدم math.noise() بدل Random للأرقام العشوائية المنظمة
9. قلل استخدام new() مع Instances، خليهم يفضلوا موجودين
10. استخدم task.defer() للأعمال مش عاجلة

النصوص والسلاسل (Strings)

1. استخدم string.format() بدل .. للتركيب المعقد

```lua
   -- كويس
   local نص = string.format("اللاعب %s عنده %d نقطة", الاسم, النقاط)
   
   -- مش كويس
   local نص = "اللاعب " .. الاسم .. " عنده " .. النقاط .. " نقطة"
```

1. string.sub() أسرع من string.match() للعمليات البسيطة
2. خلي النصوص اللي بتتكرر في متغير

```lua
   local نص_الخطأ = "هذا الحقل مطلوب"
```

الرياضيات والحسابات

1. استخدم math.clamp() عشان تحصر الأرقام في مدى معين

```lua
   local نقاط_محددة = math.clamp(النقاط, 0, 100)
```

1. math.rad() و math.deg() للتحويل بين الدرجات والراديان
2. استخدم Vector3:Cross() و :Dot() للعمليات المتجهية
3. CFrame:Lerp() للحركات السلسة بين إطارين

التعامل مع الجداول

1. استخدم table.freeze() للجداول اللي مش عايزها تتغير
2. table.clear() أسرع من عمل جدول جديد
3. استخدم next() عشان تشوف لو الجدول فاضي

```lua
   if next(الجدول) == nil then
       print("الجدول فاضي")
   end
```

الـ Roblox API

1. استخدم :GetChildren() و :GetDescendants() بحذر
2. :FindFirstChild() مع wait() أحسن من :WaitForChild() في حاجات
3. استخدم Enum لما تدور على أنواع الأشياء

```lua
   if جزء:IsA("BasePart") then
       -- تعامل معه كـ Part
   end
```

1. استخدم Attribute بدل القيم المؤقتة في الأشياء
2. حط الـ Scripts في المكان الصح: Client في LocalScript، Server في Script

الأخطاء والتديبج (Debugging)

1. استخدم assert() للتأكد من الشروط

```lua
   assert(الجزء ~= nil, "الجزء مش موجود!")
```

1. استخدم pcall() للأمان مع الدوال ممكن تفشل

```lua
   local نجح, نتيجة = pcall(function()
       return عمل_خطير()
   end)
   
   if نجح then
       print("النتيجة: " .. نتيجة)
   else
       warn("الحصل خطأ: " .. نتيجة)
   end
```

1. warn() أحسن من print() للتحذيرات
2. سجل الوقت مع os.clock()

```lua
   local بداية = os.clock()
   -- كود بطيء
   local مدة = os.clock() - بداية
   print("أخذ " .. مدة .. " ثانية")
```

1. رتب الكود وحط تعليقات بالعربي عشان تفهمه بعدين

---

٩. أمثلة عملية كاملة

مثال ١: نظام النقاط البسيط

```lua
-- في ServerScriptService
local Players = game:GetService("Players")

local نظام_النقاط = {}
نظام_النقاط.نقاط_اللاعبين = {}

function نظام_النقاط:أعط_نقاط(اسم_اللاعب, عدد_النقاط)
    if not نظام_النقاط.نقاط_اللاعبين[اسم_اللاعب] then
        نظام_النقاط.نقاط_اللاعبين[اسم_اللاعب] = 0
    end
    
    نظام_النقاط.نقاط_اللاعبين[اسم_اللاعب] += عدد_النقاط
    print(اسم_اللاعب .. " لاقط " .. عدد_النقاط .. " نقطة")
    
    return نظام_النقاط.نقاط_اللاعبين[اسم_اللاعب]
end

function نظام_النقاط:جيب_نقاط(اسم_اللاعب)
    return نظام_النقاط.نقاط_اللاعبين[اسم_اللاعب] or 0
end

return نظام_النقاط
```

مثال ٢: حركة الكاميرا السلسة

```lua
-- في LocalScript
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

local function حرك_الكاميرا(الموضع_الهدف, الإطار_الهدف, المدة)
    local بداية_الموضع = Camera.CFrame.Position
    local بداية_الإطار = Camera.CFrame
    local بداية_الوقت = os.clock()
    
    while os.clock() - بداية_الوقت < المدة do
        local النسبة = (os.clock() - بداية_الوقت) / المدة
        
        -- استخدم Lerp للحركة السلسة
        local الموضع_الحالي = بداية_الموضع:Lerp(الموضع_الهدف, النسبة)
        local الإطار_الحالي = بداية_الإطار:Lerp(الإطار_الهدف, النسبة)
        
        Camera.CFrame = CFrame.new(الموضع_الحالي) * (الإطار_الحالي - الإطار_الحالي.Position)
        
        task.wait()
    end
    
    -- تأكد من الوصول للهدف
    Camera.CFrame = CFrame.new(الموضع_الهدف) * الإطار_الهدف
end
```

---

🔗 مصادر للتعلم أكتر

· Roblox Documentation - الوثائق الرسمية
· Roblox API Reference - مرجع الـ API
· DevForum - مجتمع المطورين
· Lua 5.1 Reference - مرجع لوا الأساسي

---

💝 إذا عجبك الدليل وعايز تدعمني

الشرح ده أخذ وقت طويل وإعداد، لو حابب تدعمني عشان أكمل في المحتوى التعليمي:

🫶 ادعمني بكوباية قهوة من هنا

---

تم إعداد هذا الدليل خصيصاً للفيديو التعليمي
آخر تحديث: يناير 2026
اللغة: اللهجة المصرية مع المصطلحات الإنجليزية التقنية